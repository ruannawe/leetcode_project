#include "uthash.h"
#include <stdio.h>

int soma_numeros(unsigned short int a, unsigned short int b) { return a + b; }

int main()
{
    printf("Hello World 1! %d\n", soma_numeros(1, 2));
    return 0;
}
